import 'package:flutter/material.dart';

class ScreenAddOrder extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
       backgroundColor: Colors.white,
      body: Center(child: Text('Add your orders here')),
    );
  }
}
